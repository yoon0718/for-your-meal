주제 1
2/1
컴퓨터 바꿀 때 경로 바꾸기
emotion controller
emotion result controller
recording controller

자바에서 tensorflow 실험하기
모델 받아서 predict 돌려보기

-- 완료 --
결과 값을 받아서 쿼리문 돌릴때 = 이 아닌 포함으로 하기

-- 완료 --
정상적인 경로를 통해 접속한게 아닌
강제로 주소를 입력해서 접속했을 때
홈페이지로 강제로 이동하게 만들기

주제 2
2/2
-- 완료 --
냉장고(DB)에 들어있는 재료 중에서 하나를 랜덤 선택 후
그 재료를 사용해 만들 수 있는 요리 하나를 랜덤 선택

CREATE TABLE IF NOT EXISTS `expiration` (`ingredient` VARCHAR(255) NOT NULL, `date` VARCHAR(255));

CREATE TABLE IF NOT EXISTS `frige` (`ingredient` VARCHAR(255) NOT NULL, `date` VARCHAR(255));

INSERT INTO frige (ingredient, date) VALUES ('김치','2024-02-03');
INSERT INTO frige (ingredient, date) VALUES ('두부','2024-02-03');

2/5
-- 완료 --
카테고리 빠른 검색
-- 완료 --
카테고리 검색 일단 완료

anger 19266
disgust 9435
fear 7650
happiness 13644
neutral 15036
sadness 23124